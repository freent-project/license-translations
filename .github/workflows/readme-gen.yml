name: Generate README

on: [workflow_dispatch, push]

jobs:
  generate:
    name: README generator
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run README generator
        run: python readme_generate.py 3>&1 1>>log.txt 2>&1 || echo -e "**Error generating the README.**<br>Logs have been provided below for debugging purposes.\n\n$(cat log.txt)" > $GITHUB_STEP_SUMMARY
      - name: Push
        run: |
          git config --global user.email "actions-user@users.noreply.github.com"
          git config --global user.name "actions-user"
          git add .
          git commit -m "Generate README"
          git push
      - name: Show new README as job summary
        run: echo -e "**The new README has been generated!**<br>The README that was generated by this run can be found below.\n\n$(cat readme.md)" > $GITHUB_STEP_SUMMARY
